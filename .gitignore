node_modules/
.playwright-mcp/
*.log

# Real config lives in ~/.config/brave-agent, never in the repo.
# The templates under bridge/config.example/ are intentionally tracked.
/env
/config/
persona.local.md
