{
  "name": "brave-agent-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slack Socket Mode bridge: mention an agent, it drives your real Brave via Claude Code",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.0"
  }
}
